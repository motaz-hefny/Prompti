# Example Streamlit secrets file (TOML)
# Copy to `.streamlit/secrets.toml` locally and replace the placeholder value.
# DO NOT commit real secrets to the repository.

# Top-level key expected by the app:
GOOGLE_GENAI_API_KEY = "REPLACE_WITH_YOUR_KEY"

# Optional example of nested table format (if you prefer):
# [google]
# genai_key = "REPLACE_WITH_YOUR_KEY"
